---
type: sprint-plan
project: ebrain
program: F6 — TUI
created: 2026-07-12
modified: 2026-07-12
status: proposed
tags: [ebrain, tui, sprint, tareas, agentes, tmux]
related: [ULTRAPLAN-TUI.md, adr/ADR-003-tui-stack.md, prompts/CLAUDE-DESIGN-BRIEF.md, SPRINT.md]
---

# SPRINT — ebrain TUI (F6, tareas atómicas)

Reglas de ejecución (idénticas a F0–F5): una tarea = un worker = un resultado verificable con su comando de verify. Opus audita cada gate con `[AUDIT_PASS]` antes de avanzar de fase. Commit por fase (o por tarea si toca archivos que el auto-backup puede barrer). Ninguna tarea toca brisas-del-golfo/dekko. Toda tarea que gasta dinero declara costo estimado ANTES de correr. Workers: Sonnet o Codex; Fable 5 SOLO en los gates marcados. Pasos `[HUMANO]` se difieren al checklist, no bloquean tareas posteriores salvo que se indique.

Convención: `[ ]` pendiente · `[~]` en curso · `[x]` hecho+auditado · `[!]` bloqueado.

━━━

## FASE 6.0 — Reverse engineering de las TUIs de referencia

> Mismo método que F0 con gbrain/gstack: leer el código real (open source) u observar la conducta (cerradas), documentar en `discovery/`, sintetizar requisitos. Rubrica común para TODAS: (a) anatomía de layout (home/wordmark, prompt box, status/hint bar, footer), (b) modelo de input (keybinds, modos, palette), (c) sistema de slash commands, (d) theming/tokens, (e) modelo de sesiones/tabs, (f) stack de render y por qué, (g) qué robar / qué evitar.

- [x] 6.0.1 Clonar a `vendor/`: `sst/opencode`, `openai/codex`, `google-gemini/gemini-cli` (shallow OK — solo interesa la UI actual). Registrar SHA+versión en `discovery/00-environment.md` (append §F6). **Verify:** 3 dirs en vendor/, SHAs anotados. → SHAs: opencode `cf75036` · codex `c888e8e` · gemini-cli `f354eeb`. **Nota:** reportes en `discovery/tui/` (no `05-10`: `05` ocupado por F0).
- [x] 6.0.2 RE **OpenCode** → `discovery/tui/opencode.md` (rubrica completa; atención especial: wordmark pixel-block bicolor de la home, prompt box con borde de acento, footer `cwd:branch · versión`, hint bar `tab agents · ctrl+p commands`, su sistema de themes). Es el referente estético #1 de Eduardo. **Verify:** doc cubre (a)–(g) con archivo:línea.
- [x] 6.0.3 RE **codex** (Rust/ratatui) → `discovery/tui/codex.md` (rubrica; foco extra: cómo renderiza el stream del agente y el diff-view). **Verify:** ídem.
- [x] 6.0.4 RE **gemini-cli** (Ink/React) → `discovery/tui/gemini-cli.md` (rubrica; foco extra: por qué Ink les cuesta CPU/RAM — evidencia para ADR-003). **Verify:** ídem.
- [x] 6.0.5 RE conductual **Claude Code** (cerrada — observación de uso + docs públicas, NO decompilación) → `discovery/tui/claude-code.md` (rubrica desde conducta: statusline, spinner/verbos, palette, modos permiso/plan, `/commands`, theming). **Verify:** doc cubre (a)–(e)+(g).
- [x] 6.0.6 RE conductual **cursor-agent** (cerrada) → `discovery/tui/cursor.md` (rubrica desde conducta). **Verify:** ídem.
- [x] 6.0.7 **Síntesis** → `discovery/tui/_synthesis.md`: matriz rubrica × (5 TUIs + FlowClock); canon de keybindings propuesto para ebrain; **gap-list del tui-kit FlowClock** (qué widgets faltan: tabs, tabla, lista scrolleable, gauge, toast, …) con costo estimado por widget; insumos de refinamiento para el design brief (si los hay, actualizar `prompts/CLAUDE-DESIGN-BRIEF.md` §2). **Verify:** matriz completa + gap-list priorizada.
- [x] 6.0.8 **GATE F6.0** — auditoría Opus: ¿el kit FlowClock + tmux cubren la anatomía observada a costo razonable? → **ratificar o corregir ADR-003** (si se corrige: plan B bubbletea ANTES de escribir app). `[AUDIT_PASS]` requerido. → **`[AUDIT_PASS]` 2026-07-12**: ADR-003 RATIFICADO (Opción D; E=opentui evaluada y rechazada; decisión de Eduardo). Ver `adr/ADR-003 §Resolución` + `discovery/tui/_synthesis.md`.

## FASE 6.1 — CLI robusta (el backend contract)

> Mandato: **primero una CLI robusta; la TUI se construye encima.** Todo panel futuro = un subcomando `--json` contract-tested acá. Piezas nuevas en TypeScript/bun (`cli/`), patrón route.ts; las bash existentes ganan `--json` con jq.

- [x] 6.1.1 `ebrain status --json` (extender `harness/core/status.sh`): `{brain:{state,served_by,sources},spend:{mtd,cap,remaining},fleet:{agents:[{name,ok}]},memory:{learnings,sessions}}`. Lock-aware como hoy. **Verify:** `ebrain status --json | jq -e '.brain and .spend and .fleet and .memory'`.
- [x] 6.1.2 `ebrain doctor --json` (extender `doctor.sh`): array `checks:[{id,level:ok|warn|fail,msg}]` + `rc`. **Verify:** jq -e sobre schema; rc coherente con niveles.
- [x] 6.1.3 `ebrain spend --json` (nuevo `cli/spend.ts`): MTD por cap desde `spend.jsonl` + caps de `routing.yaml` + gap conocido de gbrain (flag `gbrain_untracked:true`). **Verify:** bun test con ledger fixture.
- [x] 6.1.4 `ebrain fleet --json` (nuevo, envuelve `install.sh --doctor` por adapter): estado por agente + clase RAM (`heavy|light`) declarada en cada `manifest.yaml` (agregar campo). **Verify:** 6 agentes listados, clases correctas.
- [x] 6.1.5 `ebrain memory recent --json` (nuevo `cli/memory.ts`): últimos N learnings de agent-memory + últimas sesiones cross-`.brain` (índice Dev Brain). Sin tocar el lock (es filesystem). **Verify:** bun test con fixtures.
- [x] 6.1.6 `ebrain sessions <list|new|peek|send|kill> --json` (nuevo `cli/sessions.ts` sobre tmux): naming `ebr-<agente>-<slug>`; `new` inyecta env/cmd del manifest del adapter; `send`/`kill` exigen `--yes`; `peek` = `capture-pane -p` (últimas N líneas, pasadas por el scrubber de secretos). **Verify:** E2E contra `scripts/fake-agent.sh` (se crea acá en versión mínima): new→list→peek→send→kill, asserts. → **HECHO**: `launch:` agregado a los 6 manifests (comando real por adapter); `EBRAIN_CALLER_CWD` (cli/ebrain) para que `new` sin `--cwd` use el dir real del caller, no el neutral de `run_bun`; deny-list cliente probado en vivo (`sessions new … --cwd .../brisas-del-golfo` → rc=2 `deny-client`); scrubber (`scrubSecrets`) redacta `KEY=valor` + prefijos conocidos (sk-ant-/sk-or-v1-/ghp_/AKIA/Bearer). E2E real contra tmux 3.2a + `scripts/fake-agent.sh`: 23/23 tests verdes (`cli/sessions.test.ts`), incluido new→list→peek→send→kill; se saltea limpio si no hay tmux (`describe.skip`).
- [x] 6.1.7 `ebrain advise "<tarea>" --json` v0 (histórico, **SUPERSDEDIDO por ADR-005/F6.6.1**): fue un clasificador de carril/agente/modelo/costo. `ebrain task-profile` es ahora el contrato canónico de señales explicables y `advise` solo su alias de compatibilidad; no elige modelo/agente ni muestra créditos/suscripciones. La estimación de routing permanece separada en `cli/model-pricing.ts` y nunca es billing.
- [x] 6.1.8 Contract-tests unificados: `tui/test/contract/` (bun test + zod) valida el schema de TODOS los `--json`; cablear al `contract-test.sh` del harness para que `ebrain doctor` lo vigile. **Verify:** suite verde; romper un campo a propósito → falla.
- [x] 6.1.9 **GATE F6.1**: `ebrain help` actualizado con los subcomandos nuevos; suite completa verde; commit de fase. `[AUDIT_PASS]`. → **`[AUDIT_PASS]` 2026-07-13 (Opus)**: 115 tests verdes; los 8 subcomandos `--json` (status/doctor/spend/fleet/memory/sessions/advise) contract-tested; auditoría de seguridad en vivo OK (scrubber redacta, deny-list rc=2, `--yes` obligatorio rc=2, frontier solo `claude_audit` con warning, `EBRAIN_CALLER_CWD` sin regresión); perf: fleet 7.6s, doctor 17s, `bun test` 1.3s. **FASE 6.1 CERRADA.**

## FASE 6.2 — Design system (Claude Design)

- [x] 6.2.1 `[HUMANO]` Pegar `docs/prompts/CLAUDE-DESIGN-BRIEF.md` en Claude Design; iterar con su checklist §3; exportar zip → descomprimir en `design-system/`; commit (vendored, read-only). **Verify:** `design-system/` contiene tokens + mockups de las 6 pantallas. → **HECHO** (Eduardo exportó): `design-system/` con `tokens/ebrain.tokens.json`, `styles.css`, `guidelines/wordmark-matrix.html`, 7 mockups en `ui_kits/ebrain/`, y componentes (Panel/TabBar/SessionCard/Gauge/Wordmark…) con `.prompt.md`+`.d.ts`.
- [x] 6.2.2 `scripts/design-sync-tui` (bun): parsea los tokens del export (JSON/CSS vars) → genera `tui/src/theme.ts`: roles semánticos (bg/surface/border/text×3/accent/ok/warn/error/info/memoria), paleta categórica por agente (8), truecolor + fallback 256, set de glifos con fallback ASCII, spacing en celdas. **Verify:** regenerable idempotente (`diff` limpio al correr 2×). → **HECHO**: generador idempotente (diff limpio 2×, sin timestamps); `makeTheme({trueColor,ascii})` produce el `color?: string` que consume el kit — `fg("accent.teal")`→`\x1b[38;2;45;212;191m` (=#2DD4BF), fallback 256→`\x1b[38;5;43m` (=xterm256 del token), passthrough de hex crudo; `spinner.frames`→normalizado a `chars` (shape uniforme); `states` (focus/blur/selected/disabled) ruteados por el mismo pipeline resolveColor.
- [x] 6.2.3 `tui/test/theme.test.ts`: todos los roles definidos; contraste mínimo aproximado texto/fondo; política **cero emoji** (Korvex discipline) verificada por regex sobre theme+glifos. **Verify:** verde. → **HECHO**: 24/24 tests; cero-emoji auditado independiente (grep de rangos emoji sobre script+theme, limpio — el worker removió `✗` U+2717 del `die()` por caer en rango). Nota de auditoría: el rango 2600–27BF del test rechazaría `✓`/`✗` que el DS sí sanciona; sin conflicto hoy (los tokens no definen glifo check/cross), revisitar si se agrega uno.
- [x] 6.2.4 **GATE F6.2**: tokens en código, `design-system/` commiteado, mockups adoptados como referencia de aceptación. `[AUDIT_PASS]`. → **`[AUDIT_PASS]` 2026-07-13 (Opus)**: `tui/src/theme.ts` = design system codificado (idempotente, consumible por el kit); `design-system/` commiteado (93 archivos, 6.2.1). **Steer de Eduardo grabado: la TUI se construye *contra* el DS, no inspirada — widgets 1:1 con los contratos `components/**/*.{d.ts,prompt.md,jsx}` (props+enums del `_adherence.oxlintrc.json`), mockups `ui_kits/ebrain/` = referencia de aceptación de las vistas, cero hex hardcodeado (todo vía theme.ts).** Suite completa 288 pass. **FASE 6.2 CERRADA.**

## FASE 6.3 — tui-kit + app shell

- [x] 6.3.1 Extraer el kit de FlowClock → `tui/src/kit/` (`screen/draw/layout/input/lineedit`, copia con atribución de origen y versión FlowClock) + package bun (`tui/package.json`, tsconfig, `bun test` cableado). **Verify:** kit compila aislado; tests de draw/layout portados verdes. → **HECHO + auditado**: 5 primitivas vendored byte-idénticas (FlowClock v3.9.0) con header de atribución; closed-set confirmado (solo `lineedit`→`input.js`); **149/149 tests** portados. Auditoría Opus: byte `\x03` (Ctrl-C) íntegro en `input.ts` (el Write tool lo había comido — el worker lo reconstruyó con `cat`+`cmp`; verificado con `parseKey("\x03")`). Gotcha: la suite tui corre por ruta explícita `bun test ./tui/test/kit` (el `root="cli"` del bunfig trata rutas sin `./` como filtro de nombre).
- [x] 6.3.2 Generalizar el kit según la gap-list de 6.0.7 — SOLO los widgets que los paneles 6.4–6.6 exigen (candidatos: TabBar, Table, ScrollList, Gauge, Toast, ConfirmDialog, Badge). Cada widget: render-a-buffer + test snapshot. **Verify:** snapshots verdes por widget. → **HECHO + auditado (2 workers ‖)**: **16 widgets** `(props, theme) → string[]` en `tui/src/widgets/{core,data,chrome,layout,brand,dialog}/` (badge·gauge·toast·spinner·table·scrolllist·sessioncard·tabbar·statusbar·hintbar·keyhint·footer·panel·terminalpeek·wordmark·confirm), theme INYECTADO. **Cada widget 1:1 con su contrato DS** (props+enums del `_adherence.oxlintrc.json`, render matcheando el `.jsx`). Auditoría Opus verificó en vivo: Panel corners redondeados `╭─╮`(no-dialog)/rectos `┌─┐`(dialog), TerminalPeek borde siempre-dim, **Wordmark `WORDMARK_MATRIX` byte-verbatim del `.jsx`** (e/b/r/a/i/n), Toast/Confirm con `✓`/`✗` (U+2713/U+2717) sancionados por el DS (no-emoji, locales con fallback ASCII). Fix Opus: header de Table `text.secondary`→`text.muted` (jsx-exact `--text-3`). Snapshots width-exacto + variante ASCII. **Cero hex hardcodeado** (solo theme.ts), cero-emoji. Suite completa: 346 pass. Nota: naming de export inconsistente entre workers (`scrolllist`/`sessioncard` minúscula vs `terminalPeek` camelCase) — la Ola 3 importa por nombre exacto.
- [x] 6.3.3 App shell (`tui/src/app.ts`): main loop, TabBar (Overview·Sessions·Memory·Routing·Fleet·Doctor), hint bar inferior estilo OpenCode (`tab paneles · / palette · ? ayuda`), footer `cwd:branch · vX.Y`, home con **wordmark pixel-block bicolor** desde theme, resize, quit limpio (restaura terminal SIEMPRE, incl. crash — handler). **Verify:** snapshot de home + navegación tab simulada. → **HECHO + auditado**: `buildFrame(state,size,theme)` puro (StatusBar→TabBar→hairline→vista→HintBar→Footer vía kit `splitV`/`splitH`) + `reduce(state,key)→{state,quit,forceRedraw}` puro + `runUi()` (alt-screen+diffFrames, resize SIGWINCH, terminal SIEMPRE restaurada: try/finally + SIGINT/SIGTERM + uncaughtException). Registry central `commands.ts` → HintBar+Footer generados de ahí (nunca hardcode). **Tabs = mockup DS `home·sessions·launch·memory·routing·doctor`** (decisión: `launch`=tab, `fleet`=telemetría StatusBar+vista doctor; reconfigura 6.5.4/6.6.1). Home replica `screens-a HomeScreen` fiel (wordmark block centrado + panel sistema/gauges + sesiones-activas focus + memorias violeta), otras 5 tabs=stubs. Kit extendido: `shift+tab` (`\x1b[Z`). 29 tests app; snapshot home 32×120, nav simulada. Cero-hex/emoji.
- [~] 6.3.3-tabs **Reconciliación pendiente con Eduardo:** el SPRINT nombraba `Overview·Fleet`; el DS-mockup manda `home·launch` (fleet→doctor). Adoptado el mockup por el steer "completamente basada en el DS". Revisar si Fleet merece vista propia en F6.5.
- [x] 6.3.4 Command palette (`/` y `ctrl+p`, portando `palette.ts` de FlowClock): registry central de comandos (mismos nombres que la CLI) + fuzzy filter. **Verify:** test de input simulado abre/filtra/ejecuta. → **HECHO (Opus directo, fallback de cuota) + auto-auditado**: `tui/src/widgets/input/commandpalette.ts` (contra `CommandPalette.jsx`: `›` prompt bold + query + caret `▌`, borde teal focus = el momento de acento, **fuzzy subsequence con matches en teal bold**, selected en `background.raised`, footer `↑↓ navegar · enter · esc`) + `tui/src/palette.ts` (state/`filterCommands`/`paletteApplyKey` portado de FlowClock, fuzzy sobre `COMMANDS`) + wiring en `app.ts` (overlay state, `/`·`ctrl+p` abren, `paletteApplyKey`→`runCommand` mapea `Command.id`→transición, compositing band-clear centrado). Verificado: `/`→palette, "ss"→sessions, enter ejecuta→tab sessions, esc cierra.
- [x] 6.3.5 Help overlay (`?`) autogenerado del registry de keybinds (nunca desincronizado). **Verify:** snapshot contiene todo keybind registrado. → **HECHO + auto-auditado**: `tui/src/help.ts` (`renderHelp` genera un dialog recto `┌─┐` agrupado nav/global desde `COMMANDS`). Test dedicado itera `COMMANDS` y asserta que **cada `key` registrado aparece** en el render → imposible desincronizar. `?` abre, esc/q/enter cierran (q NO mata el proceso dentro del overlay).
- [x] 6.3.6 `ebrain ui` en el dispatcher (`cli/ebrain`) → `bun run tui/src/app.ts` (guard: bun presente, TERM válido, tamaño mínimo 80×24 con mensaje claro). **Verify:** `ebrain ui` bootea y sale limpio en el Celeron; medir boot+RSS y anotar en `discovery/00-environment.md`. → **HECHO + auditado**: caso `ui)` en `cli/ebrain` (guard TERM no-dumb → mensaje claro + rc=1; app self-check ≥80×24; `import.meta.main`→`runUi()`). **Medido en Celeron N4120: boot 0.08–0.09s, RSS 43 MiB** (holgado bajo el presupuesto ≤100MB del ADR-003) — en `discovery/00-environment.md §F6.3.6`. Smoke sin TTY (`printf q | ebrain ui`): alt-screen enter→render→quit→exit limpio, terminal restaurada (criterio #1 en primera pasada).
- [x] 6.3.7 **GATE F6.3**: criterios #1 (boot/RSS) y #8 (cero hardcode de paleta: `grep -rn '#[0-9a-fA-F]\{6\}' tui/src --include='*.ts' | grep -v theme` vacío) medidos. `[AUDIT_PASS]`. → **`[AUDIT_PASS]` 2026-07-13 (Opus)**: #1 boot **0.10s / RSS 43.2 MiB** con overlays cargados (presupuesto ADR-003 ≤100MB, holgadísimo); #8 grep cero-hardcode **vacío** (solo theme.ts tiene hex = el DS codificado). Suite completa **399 pass**, cero-emoji, boot→quit→restore limpio. **FASE 6.3 CERRADA** — la TUI arranca, navega, palette+help funcionan, todo contra el design system.

## FASE 6.4 — Sustrato de sesiones (tmux)

- [x] 6.4.1 `tui/src/sessions/tmux.ts`: wrapper tipado sobre tmux CLI (ls/new/capture/send/kill/has-server/inside-tmux) con errores tipados (no-server, session-not-found). **Verify:** bun test contra tmux real. → **HECHO (Opus)**: REUSA el backend de `cli/sessions.ts` (list/new/peek/send/kill + scrubber, cero lógica huérfana crit#2) y solo agrega `insideTmux()`/`hasServer()`/`attachTarget()` (attach vs switch-client sin anidar). 4 tests: introspección pura + E2E real (skip limpio si no hay tmux).
- [x] 6.4.2 `scripts/fake-agent.sh` completo: banner, output periódico con timestamps, eco de stdin, señal de cierre — fixture E2E estándar del programa. **Verify:** corre 60 s bajo tmux sin morir. → **HECHO (Opus)**: trap TERM/INT/HUP (cierre limpio observable), ticks timestamped, eco de stdin (formato `recibí:` estable que asserta cli/sessions.test 6.1.6), loop sin salida salvo señal. E2E prueba supervivencia multi-tick + scrubber.
- [x] 6.4.3 Panel **Sessions**: ScrollList (nombre, agente con Badge de color categórico, cwd, uptime, estado) + pane de peek en vivo (throttle ≤1 Hz, SOLO cuando el panel está visible, **scrubber de secretos sobre cada frame**) + acciones `a` attach / `k` kill (ConfirmDialog) / `p` prompt. **Verify:** snapshot con fixture + test de throttle (no más de 1 capture/s). → **HECHO (Opus)**: contra `screens-a.jsx SessionsScreen` (Panel focus `fleet · N` + ScrollList Badge/nombre/uptime | TerminalPeek live). **Arquitectura clave:** `buildFrame` PURO (uptime precomputado, sin Date.now()); data async en slice `sessions` que el loop refresca; mutaciones (attach/kill/send/peek/refresh) por canal de **EFECTOS** en ReduceResult → `reduce` puro y 100% testeable. Throttle ≤1Hz `shouldCapture` puro y testeado. `p` = PromptBox nuevo DS-bound (contra PromptBox.jsx). `k` = ConfirmDialog danger (SOLO `y`, enter NO). Renders eyeball-verificados.
- [x] 6.4.4 Attach handoff: suspender la TUI → `tmux attach -t` (o `switch-client` si ya estamos dentro de tmux) → al volver, restaurar pantalla. **Verify:** manual guiado + estado restaurado (snapshot pre/post). → **HECHO core (Opus)**: efecto `attach` + `doAttach()` suspende alt-screen + reader + timer (`attaching` suprime repaints), cede stdio a tmux, restaura al volver; guard `disposed` evita re-entrar si hubo quit/señal durante el attach (bug de robustez cazado en self-review). Attach interactivo real (detach/switch-back en vivo) = paso **manual guiado** → checklist humano 6.4.4.
- [x] 6.4.5 Launch básico desde el panel (agente + cwd) usando el manifest del adapter (env harness completo). Probar con fake-agent Y con una sesión `claude` real → el session-log de write-back debe existir al cerrar. **Verify:** criterio #3 (rastro en `.brain/`). → **HECHO básico (Opus)**: vista `launch` = grid "agente" del mockup (subset básico; el wizard advisor+contexto es 6.6.1). ↑↓←→ selecciona, enter → efecto `launch` → gobernador → `newSession` (env del manifest, deny-client rc=2 surface). Write-back con `claude` real = **manual** → checklist humano.
- [x] 6.4.6 **Gobernador RAM**: lectura `/proc/meminfo` + clase `heavy|light` del manifest (6.1.4); 2º heavy → ConfirmDialog con MB libres reales y advertencia de la norma; registro del override en el session log. **Verify:** test con fleet fixture; manual con 1 claude vivo. → **HECHO (Opus)**: `tui/src/sessions/governor.ts` — `governLaunch()` PURO (light nunca gatea · 2º heavy → confirm · 1er heavy con RAM crítica → confirm) reusa `readClass` de fleet.ts; `parseAvailableMb` de /proc/meminfo; `countLiveHeavy` sobre listSessions; `logOverride` → `~/.config/ebrain/governor-overrides.jsonl`. Override SOLO con `y` explícito. 8 tests con fixtures. Manual con claude vivo = checklist humano.
- [x] 6.4.7 **Estudio ADR-004** (daemon HTTP-MCP compartido, dueño único del lock PGLite): spike documentado en `docs/adr/ADR-004-shared-brain-daemon.md` con GO/NO-GO/DEFER + criterios (¿gbrain serve HTTP estable? ¿migración de wiring MCP de 6 agentes? ¿qué pasa con dream 03:30?). NO se implementa en F6 salvo GO explícito de Eduardo. **Verify:** ADR con decisión argumentada. → **HECHO (Opus)**: ADR-004 escrito, **recomendación DEFER** (mismo listón que difirió Hermes: sin caso ≥2-agentes-concurrentes real, sin `serve` HTTP battle-tested, RAM 4GB; aislamiento federación/cliente gratis por proceso hoy). 4 condiciones de GO escritas. **Eduardo ratificó GO (2026-07-14)** — sobre la recomendación DEFER del maker (no auto-aprobado, patrón ADR-003). El GO abre una **fase dedicada APARTE (fuera de F6)** con esos 4 criterios como gates; ULTRAPLAN/SPRINT del daemon a planear (no bloquea el cierre de F6).
- [x] 6.4.8 **GATE F6.4** — auditoría **Fable 5** (threat model: send-keys a target equivocado, scrubber en peek, override del gobernador, superficie tmux). Criterios #3/#4/#7 en primera pasada. `[AUDIT_PASS]`. → **`[AUDIT_PASS]` 2026-07-14 (checker Fable 5 independiente, maker≠checker — NO auto-aprobado).** Loop completo: el checker devolvió `[ISSUES]` con la arquitectura sólida pero **2 gaps de seguridad reales + 3 menores**; el maker (Opus) los cerró; el checker **re-verificó sobre `3c60beb` con probes directos** (`od -c` de los bytes de send-keys, symlink real, los inputs exactos del scrubber) → `[AUDIT_PASS]`. Hallazgos cerrados: **#1** scrubber fugaba `SECRET_KEY=`/`ENCRYPTION_KEY=`/`SSH_KEY:`/`sk-proj-`/bloques PEM (cerrado: 6/6 redactan) · **#2** deny-list evadible por symlink (cerrado: `realpathSync` antes del chequeo) · **#3** send-keys sin `-l` (cerrado: `-l --` + Enter aparte) · **#4** comentario falso · **#5** governor fail-open en `unknown` (cerrado: fail-safe → gatea como heavy). Limpio confirmado: target inmutable, `y`-explícito, override logueado, terminal siempre restaurada, `buildFrame` puro. Residual LOW no bloqueante → 6.7 (ver 6.7.1). **449 tests / 0 fail. FASE 6.4 CERRADA.**

## FASE 6.5 — Paneles de conocimiento

> Plano de datos NUEVO: `tui/src/knowledge/{contracts,run}.ts`. **buildFrame sigue PURO** — solo `runUi` (impuro) invoca `run.ts`, que hace spawn de `ebrain <sub> --json` (el MISMO dispatcher contract-tested de F6.1) y delega a los parsers puros de `contracts.ts`. Cada panel ↔ un subcomando; cero lógica huérfana (los paneles NO leen `routing.yaml`/`spend.jsonl`/fs directo). Slices async en `AppState` con estado `loading/ready/error` → jamás spinner-forever.

- [x] 6.5.1 Panel **Overview** (home): wordmark + resumen de `status --json` (brain/spend gauge/fleet/memoria) + sesiones activas + últimas 3 entradas de memoria. **Verify:** snapshot con fixture. → **HECHO (Opus)**: `buildOverviewView` cablea `status --json` real (`fetchStatus`) + últimas 3 memorias (`fetchMemory(3)`) + lista bare de sesiones tmux; **statusbar global cableada a datos vivos** (antes estática "fleet 6/6") con placeholder `…/—/$—` antes del primer fetch. Boot dispara `refreshStatus`. Probado en vivo: brain up, fleet 6/6, spend real, cached=true.
- [x] 6.5.2 Panel **Memory**: learnings recientes (`memory recent --json`), búsqueda (`ebrain q`, resultado con score+source; `think` opcional con costo pintado ANTES de confirmar), form `remember` (lineedit multiline → `ebrain remember`), browse de session-logs. **Verify:** round-trip remember→recent visible (criterio #6 primera pasada). → **HECHO (Opus, alcance honesto)**: `memory recent --json` real (learnings violeta navegables + session-logs con `MM-DD HH:MM` + badge). **`r` abre overlay 'remember'** → `runRemember` → `refreshMemory` (round-trip). **Búsqueda:** `ebrain q` NO tiene `--json` → el PromptBox es informativo (passthrough honesto, sin parsear score/source inventado); `think` con costo → **diferido a 6.6** (era "opcional" en el spec). Round-trip live remember→recent = paso **manual guiado** (no ensuciar memoria permanente en CI); cubierto por reduce-test (`r`→enter emite efecto) + probe real de `fetchMemory`.
- [x] 6.5.3 Panel **Routing/Spend**: gauges MTD por cap, ledger reciente, cadenas ganador/fallback/floor por capacidad (de `advisor-rules.yaml`+`routing.yaml`), flag visible del gap gbrain-untracked. **Verify:** snapshot fixture. → **HECHO (Opus, alcance honesto)**: `spend --json` real → tabla por-cap (capacidad/rutas/mtd) navegable + gauge de presupuesto (mtd/cap, tono auto) + restante + hard-stop + **flag gbrain-untracked** visible. **Cadenas ganador/fallback/floor y ledger por-evento DIFERIDOS**: el contrato `spend --json` no expone caps por-carril ni eventos individuales; leer `routing.yaml`/`spend.jsonl` directo sería lógica huérfana que reprobaría el criterio #2 del gate → nota "pendiente contrato `routing --json`" en el panel. Misma disciplina que difirió el wizard a 6.6 / ADR-004 a fase aparte. (Follow-up: extensión estilo-F6.1 `routing --json`.)
- [x] 6.5.4 Panel **Fleet/Doctor**: `fleet --json` + `doctor --json` colorizado por nivel; `r` re-run doctor desde la UI (async, spinner). **Verify:** snapshot + re-run actualiza. → **HECHO (Opus)**: `fleet --json` + `doctor --json` reales en paralelo; checks colorizados por nivel con glifos **✓/!/✗** (sancionados DS, fallback ASCII `v/!/x` vía `theme.ascii`), navegables; panel fleet con badge categórico + online/offline + clase RAM + resumen "N warn · N fail". **`r` = `rerunDoctor`** async con spinner braille (timer dedicado ~8fps, `finally` lo limpia); landing reusa cache (doctor es lento ~17s), `r` refuerza refetch.
- [x] 6.5.5 UX de lock-awareness transversal: brain ocupado → banner "brain served by MCP (lock)" + datos cacheados con timestamp visible; NUNCA spinner infinito. **Verify:** test con fixture lock. → **HECHO (Opus)**: `overviewBanner` levanta "● brain served by `<served_by>` (lock) · datos cacheados HH:MM" cuando `status.brain.cached=true` (timestamp precomputado en el loop → buildFrame puro). **Validado en vivo: `cached=true` real** en este entorno, así que el banner se renderiza de verdad. Todo panel sin datos degrada a "cargando…"/error — nunca spinner-forever (probado en snapshots).
- [x] 6.5.6 Snapshots de TODOS los paneles con fixtures JSON puros (sin red, sin brain, sin tmux) — la suite corre en cualquier lado. **Verify:** `bun test` verde offline. → **HECHO (Opus)**: `tui/test/knowledge/contracts.test.ts` (10 tests, parsers + camino defensivo con basura) + `tui/test/knowledge/panels.test.ts` (12 tests: snapshot de los 4 paneles con fixtures + invariante de ancho + reduce de nav/acciones). `app.test.ts` actualizado (home con fixture vivo; stubs → aserción de estado-loading real). **Suite: TUI 354 + CLI 117 = 471 pass / 0 fail, offline.**
- [x] 6.5.7 **GATE F6.5**: criterio #2 completo (todo panel ↔ subcomando contract-tested, cero lógica huérfana — revisión de imports). `[AUDIT_PASS]`. → **`[AUDIT_PASS]` 2026-07-14 (SELF-AUDIT Opus — fase read-only, menor riesgo que 6.4)**. Revisión de imports: `grep` confirma que `tui/src/knowledge/` NO lee yaml/jsonl/fs; `run.ts` solo hace spawn del dispatcher `cli/ebrain`; los 5 subcomandos (status/memory/spend/fleet/doctor) están en la suite de contratos F6.1. buildFrame puro (atLabel/ts precomputados). Secret-safety: doctor muestra NOMBRES de env ("OPENAI_API_KEY set"), nunca valores; `remember` rechaza secretos en la primitiva. RSS 46.9 MiB, boot rc=0, cero-hex limpio. **Nota maker≠checker:** 6.5 es paneles read-only (sin superficie tmux/secrets de 6.4) → self-audit defensible; el gate 6.5.7 no designa Fable 5 (solo 6.0.8/6.4.8/6.7.6). Un checker Fable 5 independiente está disponible si Eduardo lo pide. Residuales LOW no bloqueantes → 6.7: (a) fetch en vuelo huérfano si se cierra durante boot; (b) doble-fetch si `r` en doctor mientras ya carga; (c) `ebrain q` search live + `think` con costo → 6.6.

## FASE 6.6 — Orquestación + advisor v1

- [x] 6.6A **OpenRouter stack visible y operable** (Codex 2026-07-15): nuevo contrato `ebrain routing --json` con capacidades, chains winner/fallback/floor, pricing verificado, gasto MTD y comando operable. Routing tab consume ese contrato y ya muestra modelos reales del stack chino, no una nota “pendiente”. **Verify:** `cli/routing.test.ts`, `cli/contract.test.ts`, `tui/test/knowledge/{contracts,panels}.test.ts`; smoke vivo `ebrain routing --json` muestra 7 capabilities.
- [x] 6.6B **Launch task router** (Codex 2026-07-15): Launch conserva el grid manual de agentes y suma composer `t` → `ebrain advise --json`; muestra task/capability/lane/agent/model/costo/razón; Enter con `one_shot_route` confirma costo y ejecuta `ebrain route --json --cap`; Enter con sesión lanza agente recomendado y envía el prompt inicial. Frontier sigue confirm-only. **Verify:** `tui/test/launch.test.ts`.
- [x] 6.6.1 **Task Profile (ADR-005):** `ebrain task-profile --json` clasifica senales/capability explicables; `advise` es alias compatible. La TUI ya no recibe una recomendacion y Enter conserva el agente manualmente seleccionado. Sin “mejor modelo”, creditos, suscripciones ni auto-routing. **Verify:** CLI 161/0, TUI 371/0, contratos/fixtures + smokes del comando y alias.
- [x] 6.6.2 **Perfiles locales + catalogo:** `profiles init` migra routing de forma explicita; `catalog-add` registra fuente/fecha y `create` guarda ordenes elegidos por el usuario. Store `700/600`, sin secretos, sin default universal. **Verify:** tests de schema/permisos + smoke temporal.
- [x] 6.6.3 **Targets OpenRouter reales:** manifests declaran selector de modelo y argv seguro; primer target = OpenCode/OpenRouter (`--model` comprobado). Un adapter sin soporte no ofrece selector. `targets launch` deja atribucion `untracked`, nunca USD/tokens inventados. **Verify:** tests de argv/selector/denegaciones + launcher tmux existente, sin llamada pagada.
- [x] 6.6.4 **Launch Wizard completo:** `w` carga targets/perfiles contractuales; seleccion explicita, capability ajustable, cwd editable, preview de norms/memoria/MCP/workflow/RAM/modelo y doble confirmacion RAM. `r` ya no crashea tras navegar el grid. **Verify:** reducer/render + contracts + suites completas.
- [x] 6.6.5 **Prompt composer + evidencia:** Sessions `p` abre composer multilinea (paste preservado o Alt+Enter), Enter previsualiza y solo `y` entrega los bytes exactos por `sendToSession`; el draft queda exclusivamente en memoria y no entra a historial/telemetria. `cli/benchmark-evidence.ts` define schema opcional estricto con source/as_of/version/task_scope/metricas, sin winner/ranking/politica de routing. **Verify:** reducer payload exacto, multilinea, cancelacion y privacidad; parser rechaza campos secretos/routing. Fix adicional: Launch manual toma su task del slice existente, eliminando `initialPrompt` indefinido; `w` explica el `profiles init --yes` requerido y Tab enfoca target/perfil correctamente.
- [x] 6.6.6 Fixture de **10 tareas canónicas:** `cli/task-profile.fixtures.ts` cubre coding, agentic, web design, long context, terminal y general con diez tareas versionadas. El test valida señales literales, capability y modos compatibles; prohíbe campos de agente/modelo/ranking/costo. **Verify:** 10/10 sin ranking implícito. Gotchas visibles y cubiertos: `script` coincide dentro de `TypeScript`; `tool` no sustituye el keyword específico `tool-call`.
- [ ] 6.6.7 **GATE F6.6 (ADR-005)**. `[AUDIT_PASS]`.
- [x] 6.6C/D **Workflows/skills + learning loop:** contrato `ebrain workflows`, panel Memory (browse/materialize/attach), captura de candidatos y skillify con `--yes`; ver `docs/SPRINT-ORCHESTRATION.md` y `WORKFLOW-LEARNING-LOOP.md`.
- [x] 6.6E **Cost ledger v2:** `ebrain cost --json` normaliza tokens/USD por provider/agente/modelo/sesión/workflow; Routing `c` abre la vista Cost. No se infiere costo de suscripciones; ver `COST-LEDGER.md`.

## FASE 6.7 — Hardening + ship

- [x] 6.7.1 Edge cases con test o guard: tmux sin server / muerto a mitad de peek; lock PGLite; sin red (paneles degradan); terminal < 80×24; TERM sin truecolor (fallback 256 automático); crash → terminal SIEMPRE restaurada. **Verify:** `docs/TUI-EDGE-CASES.md` enlaza cada guard a su prueba; nueva regresión limpia pane stale y muestra error si tmux muere entre list/peek. El restore ante señal/crash queda además en checklist manual final. **Residual del gate 6.4.8 (LOW, no bloqueante):** una llave PEM que cruce el borde de la ventana de captura (200 líneas) puede dejar líneas de body base64 visibles (header presente + END fuera de ventana, o viceversa). Está detrás de `guard-secrets.sh`; cerrarlo del todo exige redactar bloques base64 huérfanos → sobre-redacción masiva de output legítimo (JWT/hashes). Documentado para la auditoría final.
- [x] 6.7.2 Perf en el Celeron: `docs/f6-success-criteria.md` registra boot 0.08-0.10 s, RSS F6.3 43 MiB y pico F6.7 Sessions 47 MiB; CPU idle ~0.6% y peek 1 Hz ~1.8% de un core (muestra local de 5 s). D8 decidido **no**: `bun build --compile` no aporta beneficio medido y agrega carga de release. **Verify:** criterio #1 con evidencia de maker; aceptación final pendiente de GPT-5.6-sol.
- [x] 6.7.3 Docs: `tui/README.md` documenta los keybindings reales por vista, el composer, signals y el wizard first-use; `docs/runbook.md` enlaza la operación TUI. `?` sigue siendo la ayuda generada desde `COMMANDS` y la hint bar expone controles contextuales. **Verify:** docs en inglés y coherentes con `commands.ts`/reducer.
- [x] 6.7.4 `docs/f6-success-criteria.md`: **8/8 criterios con evidencia** (formato F5), actualizado para ADR-005: las diez tareas validan señales/modos, no un ganador. Cada fila distingue evidencia automática de los dos pasos humanos inevitables (write-back Claude y round-trip cross-agent). **Verify:** tabla completa; aceptación final pendiente de GPT-5.6-sol.
- [ ] 6.7.5 Retro → lecciones a `05-knowledge/permanent-notes/` del vault + `ebrain remember` por cada lección reusable. **Verify:** nota commiteada.
- [ ] 6.7.6 CHANGELOG ebrain + vault, commits finales, **GATE F6.7 — auditoría Fable 5 final del programa**. `[AUDIT_PASS]`.
- [ ] 6.7.7 Checklist humano F6 (append a `docs/human-checklist.md`): aceptación visual vs mockups, decisión ADR-004 si quedó DEFER, y primer día real de uso como daily driver con notas de fricción.

━━━

**Dependencias entre fases:** 6.0 → gate ratifica ADR-003 → 6.1 ‖ 6.2 (paralelizables) → 6.3 → 6.4 → 6.5 → 6.6 → 6.7. El paso humano 6.2.1 (Claude Design) puede correr en paralelo con 6.1 sin bloquear nada hasta 6.3.
