# CKIS secret-scan allowlist (repo-root). One path-glob per line; '#' comments ok.
# Paths here legitimately QUOTE token shapes and are NOT real secrets.
#
# cli/sessions.test.ts — unit test for scrubSecrets(): feeds FAKE sk-/ghp_/AKIA
#   fixtures and asserts the redactor masks them. The entropy scanner flags the
#   fake tokens, but they are test data, not live credentials. (aedneth/ebrain)
cli/sessions.test.ts
