Prompt de entrada: borde izquierdo grueso \u2503 teal (el momento de acento tipico de una vista), fondo `--surface-1`, caret \u258C.

```jsx
<PromptBox value="lanza un refactor del router" rows={2} hint="enter lanzar" />
<PromptBox focus={false} placeholder="buscar en memoria\u2026" />
```

- blur -> barra `--border-1` y sin caret. Placeholder siempre dim.
