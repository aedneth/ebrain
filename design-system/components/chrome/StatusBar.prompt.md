Barra de estado superior sobre `--surface-1`: identidad a la izquierda, telemetria a la derecha.

```jsx
<StatusBar
  left={<Wordmark variant="compact" />}
  right={<span>brain <span style={{color:'var(--ok)'}}>UP</span><StatusSep />$2.1/$10</span>}
/>
```
