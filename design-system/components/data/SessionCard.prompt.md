Fila de sesion agentica: badge categorico + nombre + uptime dim + estado semantico.

```jsx
<SessionCard agent="claude" name="ebr-claude-korvex" uptime="02:41" state="running" selected />
<SessionCard agent="gemini" name="ebr-gem-web" uptime="00:12" state="waiting" detail="esperando confirmacion frontier" />
```

- Estados: running ok / waiting warn / idle dim / error / done.
